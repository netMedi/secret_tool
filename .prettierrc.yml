printWidth: 80
trailingComma: all
arrowParens: avoid
overrides:
  - files:
      - "*.ts"
      - "*.tsx"
    options:
      parser: typescript
  - files:
      - "*.json"
    options:
      parser: json
  - files:
      - "*.yml"
      - "*.yaml"
    options:
      parser: yaml
  - files:
      - "*.md"
    options:
      parser: markdown
